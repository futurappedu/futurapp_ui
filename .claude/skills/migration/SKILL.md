---
name: migration
description: Not applicable — this project uses a backend REST API on Railway, not Supabase. Database migrations are managed on the backend.
---

# Database Migration

## Not Applicable

This project is a **frontend-only React SPA**. The database is managed by the backend API hosted on Railway.

If you need to make database changes:

1. Coordinate with the backend team
2. Changes are made in the backend repository, not here
3. Frontend may need to update API calls or types after backend changes

This skill was inherited from another project and is kept as a placeholder. Use `/deploy` for frontend deployments instead.
